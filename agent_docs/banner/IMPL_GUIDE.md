# 🎨 Banner Implementation Guide (구현 가이드)

## 1. 구조 설계
- **공통 인터페이스**: `Banner` 인터페이스를 정의하여, `Banner2020`, `Banner2018` 엔티티가 이를 구현하도록 합니다. Service에서는 `Banner` 인터페이스로 로직을 처리하여 중복을 줄입니다.

## 2. 구현 전략
- **이미지 업로드**: `admin/IMPL_GUIDE.md`의 파일 업로드 전략을 따릅니다.
- **캐싱 (Caching)**: 배너는 조회 빈도가 높고 변경이 적으므로, `Reddis` 또는 `@Cacheable` 적용을 고려합니다. (선택 사항)

