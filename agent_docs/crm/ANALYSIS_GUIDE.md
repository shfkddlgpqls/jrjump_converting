# 📞 CRM Analysis Guide

## 1. 분석 포인트
- **팝업 로직**: CRM은 독립적인 페이지보다 팝업(`window.open`) 형태가 많습니다. 파라미터 전달 방식(`opener` 참조 등)을 분석하십시오.
- **외부 연동**: SMS/메일 발송 시 실제 발송 데몬(Daemon)이나 외부 API를 호출하는지, 단순히 DB에 `INSERT`만 하는지 확인해야 합니다.

## 2. 핵심 정책 발굴 (Core Policy Discovery) - **Mandatory**
단순 코드 변환을 넘어, **기획자가 의도한 비즈니스 규칙(Business Rule)**을 찾아내야 합니다.

- **발송 제한 정책**: "야간(21:00~08:00) 발송 금지는 법적 규제 때문인가, 운영 정책인가?"
- **권한 정책**: "상담원 등급별로 고객 정보 노출 범위가 다른 이유는 무엇인가?" (개인정보보호 정책 등)

> **Output**: 발견된 정책은 **반드시** `agent_docs/crm/POLICY.md` 파일에 기록해야 합니다.

## 3. 체크리스트
- [ ] `CRM_SMS` 테이블에 `INSERT` 시 트리거가 동작하는지 확인했는가?
- [ ] 상담원(`CRM_MANAGER`) 권한과 일반 관리자(`admin`) 권한의 차이를 파악했는가?
