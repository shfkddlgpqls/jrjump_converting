# 🔧 Admin Analysis Guide

## 1. 분석 포인트
- **공통 인클루드**: `admin/inc/header.php`, `_config.php` 등 모든 페이지에 포함되는 공통 로직을 먼저 분석하여, 권한 체크나 DB 연결 방식을 파악하십시오.
- **파라미터 패턴**: `exec`, `exec2` 등의 파라미터로 분기 처리하는 `Switch-Case` 문을 주의 깊게 분석하십시오.

## 2. 핵심 정책 발굴 (Core Policy Discovery) - **Mandatory**
단순 코드 변환을 넘어, **기획자가 의도한 비즈니스 규칙(Business Rule)**을 찾아내야 합니다.

- **책임과 권한**: "슈퍼 관리자와 일반 관리자의 권한 구분 기준은 무엇인가?" (예: 결제 취소는 팀장급만 가능 등)
- **감사 정책**: "어떤 행위가 반드시 로그로 남아야 하는가?" (개인정보 조회, 엑셀 다운로드 등 보안 감사 요건)

> **Output**: 발견된 정책은 **반드시** `agent_docs/admin/POLICY.md` 파일에 기록해야 합니다.

## 3. 체크리스트
- [ ] 관리자 로그인 세션 처리 방식을 파악했는가?
- [ ] 메뉴 권한 관리(레벨별 접근 제한) 로직을 확인했는가?
